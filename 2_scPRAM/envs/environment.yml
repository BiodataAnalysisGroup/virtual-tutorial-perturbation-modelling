name: scPRAM
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
    - scpram
    - scvi-tools
    - jupyter
    - scanpy
prefix: /home/g0bi/miniforge3/envs/scPRAM
#change the prefix line in function of your env location
